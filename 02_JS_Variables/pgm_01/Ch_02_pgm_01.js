//Removed the semicolon to observe the error

var score

//Adding the semicolon back

var score;

//Declaring two more variables

var playerName;
var highScore;

console.log(score);
console.log(playerName);
console.log(highScore);